    package com.creo.generated.service.controller; // Festes Paket

    import org.springframework.http.ResponseEntity;
    import org.springframework.http.HttpStatus;
    import org.springframework.web.bind.annotation.*;
    import java.util.Map;
    import java.util.List;

    {{#each this.handler_func_imports}}
    {{{this.import}}};
    {{/each}}

    {{#each this.service_call_imports}}
    {{{this.import}}};
    {{/each}}

    {{#each this.type_imports}}
    {{{this.import}}};
    {{/each}}

    @RestController
    @RequestMapping("/") // Fester Basis-Pfad
    public class ServiceController { // Fester Controller-Klassenname

        {{#each this.http_get_operations}}
        {{> get_operation_spring this}}
        {{/each}}

        {{#each this.http_post_operations}}
        {{> post_operation_spring this}}
        {{/each}}
    }
    